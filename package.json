{
  "name": "working-with-electron",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron main.js",
    "rebuild": "electron-rebuild -f -w systeminformation",
    "pack": "electron-builder --dir",
    "dist": "electron-builder"
  },
  "author": "abdurahmon",
  "license": "ISC",
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "diskusage": "^1.2.0",
    "systeminformation": "^5.21.20"
  },
  "build": {
    "win": {
      "target": "portable"
    }
  }
}
